# 🚀 Инструкция по запуску School Auto Chat

## ✨ Новые возможности:
- 🔐 Регистрация и вход с паролем
- 🎨 Уникальный цвет для каждого пользователя
- 🌐 Полная поддержка иврита (RTL), русского, английского

---

## Для первого запуска:

1. **Откройте PowerShell или CMD в этой папке**

2. **Создайте виртуальное окружение (если не создано):**
```bash
python -m venv .venv
```

3. **Активируйте виртуальное окружение:**
```bash
.venv\Scripts\activate
```

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

## Запуск сервера:

### Способ 1: Через BAT файл (самый простой)
Просто дважды кликните на файл `run.bat`

### Способ 2: Через командную строку
```bash
.venv\Scripts\activate
python server.py
```

## После запуска:

1. Откроется консоль с информацией о сервере
2. Найдите строку с вашим IP-адресом
3. Откройте браузер и перейдите по адресу `http://127.0.0.1:5000`

### Первый вход:
1. **Выберите язык** (עברית / Русский / English) в правом верхнем углу
2. **Зарегистрируйтесь:**
   - Введите имя пользователя (минимум 3 символа)
   - Введите пароль (минимум 4 символа)
   - Нажмите "Зарегистрироваться"
3. **Войдите** с вашими учетными данными

### 🎨 Ваш цвет:
Каждому пользователю автоматически присваивается уникальный цвет, который будет виден всем!

### 🌐 Иврит (עברית):
При выборе ивр:
1. **Зарегистрироваться** на том же сервере
2. Открыть в браузере: `http://[ВАШ_IP]:5000`
3. Выбрать язык интерфейса
4. Зарегистрироваться со своим именем и паролем
5. Войти и начать общение!

Например: `http://192.168.1.10:5000`

**Важно:** Каждый пользователь может войти только один раз!

---

## 📂 База данных пользователей:

Файл `users_db.json` автоматически создается и хранит всех зарегистрированных пользователей.
Не удаляйте этот файл, если хотите сохранить аккаунты!
ipconfig
```

Ищите строку "IPv4 Address" - это ваш локальный IP (например: 192.168.1.10)

## Подключение учеников:

Ученики должны открыть в браузере:
```
http://[ВАШ_IP]:5000
```

Например: `http://192.168.1.10:5000`

---

## 🐛 Если что-то не работает:

**Ошибка: "python не является внутренней командой"**
- Установите Python с официального сайта python.org

**Ошибка: "No module named flask"**
- Убедитесь, что виртуальное окружение активировано
- Запустите: `pip install -r requirements.txt`

**Порт занят:**
- Откройте `server.py`
- Измените строку `port=5000` на другой порт (например, 5001)

---

**✅ Готово! Теперь можно тестировать в Visual Studio Code или запускать сервер!**
